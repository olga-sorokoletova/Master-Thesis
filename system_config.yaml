system:
  nginx: on
  nginx_port: 80    # nginx server port

simulator:
  stage: vnc        # off|on|x11|vnc|x11-nvidia-todo
  vnc_port: 3000    # vnc server port

robot:
  motorboard: off   # off|arduino|ln298|pka03|marrtino2019
  4wd: off
  joystick: off
  laser: off        # off | hokuyo | rplidar
  camera: off       # off | usbcam | astra | xtion
  camera_resolution: (640, 480)   # (width, height)

functions:
  navigation: cohan   # off | on | cohan
  vision: off
  speech: off
  mapping: off
  objrec: off
  social: off

